function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(20);
}

function draw() {
if (mouseIsPressed) {
fill(50, 100, 200, 100);//semi-transparent blue
noStroke();
ellipse(mouseX, mouseY, 20, 20)//draw a circle at mouse
 }
}